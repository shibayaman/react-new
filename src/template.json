{
  "devDependencies": [
    "@babel/core",
    "@babel/preset-env",
    "@babel/preset-react",
    "babel-loader",
    "webpack",
    "webpack-cli",
    "webpack-dev-server"
  ],
  "dependencies": ["react", "react-dom"],
  "scripts": {
    "build": "webpack --env.NODE_ENV=production && cp ./public/index.html ./build",
    "dev": "webpack-dev-server --env.NODE_ENV=development"
  }
}
